{
  "name": "educaweb",
  "version": "1.0.0",
  "description": "Sistema de gestão escolar com IA",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}